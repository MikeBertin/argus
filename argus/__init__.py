"""ARGUS — packet-capture detection-rule engine.

The hundred eyes that never sleep: every flow inspected, ranked by severity,
confidence and MITRE ATT&CK technique. Detections are auto-discovered Python
rule plugins (see ``argus.rules``).
"""

__version__ = "0.1.0"
