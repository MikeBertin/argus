"""Detection rule plugins, auto-discovered by ``argus.loader``.

Each module defines one or more concrete ``argus.models.Rule`` subclasses.
Drop a new file here and it is picked up automatically — no registration.
"""
